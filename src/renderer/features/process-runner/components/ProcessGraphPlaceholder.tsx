export function ProcessGraphPlaceholder() {
  return (
    <section className="graph-placeholder">
      <p className="eyebrow">Process Graph</p>
      <h2>Runtime graph mode</h2>
      <p>
        This mode will show agent hierarchy, workflow nodes, dry-run results, approvals, and
        dependencies. The universal left strip stays available while modes change.
      </p>
    </section>
  );
}
